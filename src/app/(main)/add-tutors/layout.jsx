export const metadata = {
    title: "Add Your Tutor",
    description: "Fill in the info of the tutor and add them to MediQueue database.",
};

const AddTutorLayout = ({ children }) => {
    return children;
};

export default AddTutorLayout;

